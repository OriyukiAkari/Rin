# Rin pre-v1.0 baseline

This document seals the local Rin state before the v1.0 hardening work begins.

## Identity

- Baseline date: 2026-08-14 (Asia/Shanghai)
- Source version: 0.3.0
- Upstream base: `9efc4fd`
- Baseline tag: `rin-baseline-v0.3.0-20260814`
- Successor branch: `v1.0-hardening`

The baseline includes two local development fixes made while restoring the project:

- password-only local development is accepted when GitHub OAuth is not configured;
- Ctrl+C is treated as a normal development-server shutdown.

## Verified state

- 407 tests passed with 0 failures.
- Client, server, CLI, and shared-package TypeScript checks passed.
- The React production build passed.
- The Cloudflare Worker dry-run build passed.
- Local D1 is at migration version 10 and `PRAGMA integrity_check` returned `ok`.

## Scope of the v1.0 successor

The v1.0 line starts from this tag and is responsible for the security, data-integrity,
API-contract, migration, deployment, content-safety, performance, and test-quality issues
recorded during the 2026-08-14 source audit. This baseline is evidence of the pre-fix state;
it must not be moved after creation.
